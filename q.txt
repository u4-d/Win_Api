// сроб
    MoveToEx(hdc, centerX + diagonalDistance - halfLength,
             centerY + diagonalDistance + halfLength, nullptr);
    LineTo(hdc, centerX + diagonalDistance + halfLength,
           centerY + diagonalDistance - halfLength);

// вСоб
    MoveToEx(hdc, centerX - diagonalDistance - halfLength, centerY + diagonalDistance - halfLength, nullptr);
    LineTo(hdc, centerX - diagonalDistance + halfLength, centerY + diagonalDistance + halfLength);